1. Check if a file is present, If it present delete the file and create a new file.
2. In a directory print all the file names (Not directory names).
3. Delete all the files and directories from a folder.